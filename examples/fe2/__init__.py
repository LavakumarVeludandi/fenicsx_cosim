"""Self-contained FE² example package for fenicsx-cosim.

Deliberately has **no dependency on sdlab** so that fenicsx-cosim stays a
standalone, pip-installable package. The RVE physics (two-phase J2 plasticity
under KUBC) is implemented here from scratch.
"""
